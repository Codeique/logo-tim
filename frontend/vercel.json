{
  "installCommand": "npm install --no-package-lock --force",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://logo-tim-production.up.railway.app/api/:path*"
    }
  ]
}

# Vercel environment variables
# Set these in the Vercel dashboard under Settings → Environment Variables

# URL of the Railway backend (used by Socket.io for direct WebSocket connection)
# API calls go through Vercel rewrites (see vercel.json), so they don't need this
VITE_BACKEND_URL=https://your-app.railway.app
